{% extends 'layouts/app.html.twig' %}

{% block page_title %}Categories d'equipement{% endblock %}
{% block page_subtitle %}Gestion des categories{% endblock %}

{% block page_actions %}
    <a href="{{ path('app_categorie_equipement_new') }}"
       class="bg-primary hover:bg-primary/90 text-white px-4 py-2 rounded-lg font-medium transition flex items-center space-x-2">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
        </svg>
        <span>Nouvelle categorie</span>
    </a>
{% endblock %}

{% block page_content %}
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
        <div class="bg-white dark:bg-fonddeux rounded-2xl shadow-md dark:shadow-none border border-gray-200 dark:border-white/10 p-4 kpi-accent-primary">
            <p class="text-sm text-gray-600 dark:text-gray-300 font-medium">Total categories</p>
            <p class="text-2xl font-bold text-gray-800 dark:text-white mt-1">{{ pagination.totalItemCount }}</p>
        </div>
    </div>

    <div class="bg-white dark:bg-fonddeux rounded-2xl shadow-md dark:shadow-none border border-gray-200 dark:border-white/10 overflow-hidden">
        <div class="px-6 py-4 border-b border-gray-200 dark:border-white/10 bg-gray-50 dark:bg-fondun">
            <p class="text-sm font-medium text-gray-700 dark:text-gray-200">{{ pagination.totalItemCount }} categories trouvees</p>
        </div>
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-gray-50 dark:bg-fondun border-b border-gray-200 dark:border-white/10">
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Nom</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Description</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Equipements</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                </tr>
                </thead>
                <tbody class="bg-white dark:bg-fonddeux divide-y divide-gray-200 dark:divide-white/10">
                {% for categorie in pagination %}
                    <tr class="hover:bg-gray-50 dark:hover:bg-white/5 transition">
                        <td class="px-6 py-4 whitespace-nowrap">
                            <div class="text-sm font-medium text-gray-900 dark:text-white">{{ categorie.nom }}</div>
                        </td>
                        <td class="px-6 py-4 text-sm text-gray-700 dark:text-gray-200">{{ categorie.description ?: '-' }}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700 dark:text-gray-200">{{ categorie.equipements|length }}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                            <div class="flex items-center gap-4">
                                <a href="{{ path('app_categorie_equipement_show', {'id': categorie.id}) }}"
                                   class="text-primary hover:text-primary/80">Voir</a>
                                <a href="{{ path('app_categorie_equipement_edit', {'id': categorie.id}) }}"
                                   class="text-gray-700 dark:text-gray-200 hover:text-gray-900 dark:hover:text-white">Modifier</a>
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="px-6 py-10 text-center text-gray-500 dark:text-gray-400">Aucune categorie trouvee.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    {% if pagination.pageCount > 1 %}
        <div class="mt-6 flex justify-center">
            <nav class="relative z-0 inline-flex rounded-xl shadow-sm -space-x-px overflow-hidden">
                {% if pagination.currentPageNumber > 1 %}
                    <a href="{{ path('app_categorie_equipement_index', {page: pagination.currentPageNumber - 1}) }}"
                       class="relative inline-flex items-center px-3 py-2 border border-gray-300 dark:border-white/10 bg-white dark:bg-fonddeux text-sm font-medium text-gray-500 dark:text-gray-400 hover:bg-gray-50 dark:hover:bg-white/5">
                        Precedent
                    </a>
                {% endif %}
                {% for page in 1..pagination.pageCount %}
                    <a href="{{ path('app_categorie_equipement_index', {page: page}) }}"
                       class="relative inline-flex items-center px-4 py-2 border border-gray-300 dark:border-white/10 bg-white dark:bg-fonddeux text-sm font-medium {{ page == pagination.currentPageNumber ? 'text-fondcinq bg-fondcinq/10 dark:bg-fondcinq/15 dark:text-fondcinq' : 'text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-white/5' }}">
                        {{ page }}
                    </a>
                {% endfor %}
                {% if pagination.currentPageNumber < pagination.pageCount %}
                    <a href="{{ path('app_categorie_equipement_index', {page: pagination.currentPageNumber + 1}) }}"
                       class="relative inline-flex items-center px-3 py-2 border border-gray-300 dark:border-white/10 bg-white dark:bg-fonddeux text-sm font-medium text-gray-500 dark:text-gray-400 hover:bg-gray-50 dark:hover:bg-white/5">
                        Suivant
                    </a>
                {% endif %}
            </nav>
        </div>
    {% endif %}
{% endblock %}
